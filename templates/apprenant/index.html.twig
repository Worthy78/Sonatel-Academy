{% extends 'base.html.twig' %}

{% block title %}Apprenant index{% endblock %}

{% block body %}
    <h1>Apprenant index</h1>

   <!-- <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Adresse</th>
                <th>Email</th>
                <th>Description</th>
                <th>DateNaiss</th>
                <th>Photo</th>
                <th>Cv</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
            <tr>
              {#--  <td>{{ apprenant.id }}</td>
                <td>{{ apprenant.nom }}</td>
                <td>{{ apprenant.prenom }}</td>
                <td>{{ apprenant.adresse }}</td>
                <td>{{ apprenant.email }}</td>
                <td>{{ apprenant.description }}</td>
                <td>{{ apprenant.dateNaiss ? apprenant.dateNaiss|date('Y-m-d') : '' }}</td>
                <td>{{ apprenant.photo }}</td>
                <td>{{ apprenant.cv }}</td>
                <td>
                    <a href="{{ path('apprenant_show', {'id': apprenant.id}) }}">show</a>
                    <a href="{{ path('apprenant_edit', {'id': apprenant.id}) }}">edit</a>
                </td>#}
            </tr>

            <tr>
                <td colspan="10">no records found</td>
            </tr>

        </tbody>
    </table>-->
    <div class="py-5 bg-cover text-white" data-dark-overlay="5" style="background:url(assets/img/1920/658_2.jpg) no-repeat">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h2>Blog</h2>
                </div>
                <div class="col-md-6">
                    <ol class="breadcrumb justify-content-md-end bg-transparent">
                        <li class="breadcrumb-item">
                            <a href="blog-standard.html#">Accueil</a>
                        </li>
                        <li class="breadcrumb-item">
                            <a href="blog-standard.html#"> Blog</a>
                        </li>
                        <li class="breadcrumb-item">
                            Tous les postes
                        </li>
                    </ol>
                </div>
            </div>
        </div>
    </div>



    <section class="pt-5 paddingBottom-100">
        <div class="container">
            <div class="row">
                {% for apprenant in apprenants %}

                <div class="col-lg-4 col-md-6 marginTop-30">
                    <article class="card">
                        <div class="card-img">
                                <a href="{{ path('apprenant_show', {'id': apprenant.id}) }}">
                                <img class="rounded w-100" src="/front/assets/img/blog/standard/1.jpg" alt="">
                            </a>
                        </div>
                        <div class="card-body px-0">
                            <a class="text-primary" href="{{ path('apprenant_show', {'id': apprenant.id}) }}">{{ apprenant.fili�ere.libelle }}</a>
                            <a href="{{ path('apprenant_show', {'id': apprenant.id}) }}" class="h4 my-2">
                                {{ apprenant.prenom }} {{ apprenant.nom }}
                            </a>
                            <p>
                                28 Mar, 2018 - by <a class="text-primary" href="{{ path('apprenant_show', {'id': apprenant.id}) }}">Alex</a>
                            </p>
                        </div>
                    </article>
                </div> <!-- END col-lg-4 col-md-6 -->
{% endfor %}
                <div class="col-12 marginTop-70">
                    <ul class="pagination pagination-primary align-items-center justify-content-center">
                        <li class="page-item mx-2">
                            <a href="blog-standard.html#">
                                <i class="ti-arrow-left small mr-2"></i>
                                Previous
                            </a>
                        </li>
                        <li class="page-item mx-2 font-weight-bold">2/45</li>
                        <li class="page-item mx-2">
                            <a href="blog-standard.html#">
                                Next
                                <i class="ti-arrow-right small ml-2"></i>
                            </a>
                        </li>
                    </ul>
                </div>
            </div> <!-- END row-->
        </div> <!-- END container-->
    </section>
    <a href="{{ path('apprenant_new') }}">Create new</a>
{% endblock %}
